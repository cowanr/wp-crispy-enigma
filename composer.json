{
    "require": {
        "brain/monkey": "2.*"
    },
	"scripts" : {
    	"unit-tests": "phpunit --testsuite=unit",
    	"wp-tests": "phpunit --configuration phpunit-integration.xml.dist --testsuite=integration"
	}
}
